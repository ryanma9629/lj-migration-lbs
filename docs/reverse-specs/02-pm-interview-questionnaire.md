# 旧系统产品经理访谈问卷

## 1. 访谈记录信息

| 项目 | 内容 |
| --- | --- |
| 访谈日期 |  |
| 访谈对象 |  |
| 访谈对象角色 |  |
| 记录人 |  |
| 是否了解原生产运行方式 | 是 / 否 |
| 是否了解原始需求背景 | 是 / 否 |

## 2. 关键数据上下文速查

本节用于给访谈对象提供“问题对应到旧系统哪里”的线索。
访谈时可以只挑相关行给产品经理参考，不需要逐项讲解数据库结构。

| 业务概念 | 代码/数据线索 | 当前逆向理解 | 需要访谈确认点 |
| --- | --- | --- | --- |
| 通用认领状态 | `IBS.T1_VUCH_EMP_RELA.CLAIM_STATUS_ID` | 存款、贷款等凭证类认领主表中的认领状态 | 各状态码是否全局统一 |
| 贵金属认领状态 | `IBS.GOLD_EMP_RELA.CLAIM_STATUS_ID` | 贵金属认领链路中的认领状态 | 是否沿用通用状态码 |
| 基金认领状态 | `IBS.T1_FUND_TXN_EMP_RELA.CLAIM_STATUS_ID`、`IBS.T1_FUND_CST_EMP_RELA.CLAIM_STATUS_ID`、`IBS.T1_FUND_DT_EMP_RELA.CLAIM_STATUS_ID` | 基金交易、基金客户、基金定投三类认领状态 | 三类基金状态是否同口径 |
| 代发工资认领状态 | `IBS.T1_DF_SLRY_EMP_RELA.CLAIM_STATUS_ID` | 代发工资认领状态 | 是否与通用认领一致 |
| 第三方存管认领状态 | `IBS.T1_THD_EMP_RELA.CLAIM_STATUS_ID` | 第三方存管认领状态 | 是否与通用认领一致 |
| 保险认领状态 | `IBS.T1_YBDB_EMP_RELA.CLAIM_STATUS_ID` | 保险/银保类认领状态 | 是否与通用认领一致 |
| 调整状态 | `IBS.T1_VUCH_EMP_RELA.RVS_STATUS_ID` | 已认领记录后续调整状态 | 调整状态是否与认领状态复用口径 |
| 调整日志状态 | `IBS.T6_VUCH_EMP_RELA_CHANGE_LOG.NEW_CLAIM_STATUS_ID` | 调整日志里的新认领状态 | 是否代表调整审批链路状态 |
| 认领状态字典 | `IBS.T9_CLAIM_STATUS_TYPE` | 页面常用的认领状态字典表 | 是否为原生产标准字典 |
| 调整状态字典 | `IBS.T9_RVS_STATUS_TYPE` | 页面常用的调整状态字典表 | 是否为原生产标准字典 |
| 营销活动编号 | `MARKETING_NO`，常见特殊值 `2016qq` | 某些认领/查询中会排除或特殊处理 | 是否为真实业务活动口径 |
| 虚拟/机构归属对象 | `EMP_ID='000000'`，或 `EMP_ID` 存机构号 | 可能代表未认领占位、公共池或机构归属 | 真实业务含义 |
| 自动审批标识 | `VERIFY_EMP_ID='无需审批'`、`ADMIN_HK`、`ADMIN_SZ` | 可能代表无需人工审批、换卡自动审批、事中自动审批 | 是否为正式业务规则 |
| 考核期字段 | `EXAM_YEAR`、`EXAM_Q`、`STUS` | 考核、评分、奖金等模块使用 | 考核期控制范围 |
| 审计备注 | `REMARK`、`REMARK1` | 拼接认领时间、认领工号、审批时间、审批理由等 | 迁移时是否结构化保留 |

## 3. 系统定位与业务目标

### 3.1 这套系统最核心的业务目标是什么？

`优先级：P0`

A. 对各类业务业绩进行“人员归属”认定  
B. 对各类业务业绩进行“机构归属”认定  
C. 同时支持人员归属和机构归属  
D. 主要是审批台账，归属只是其中一部分  
E. 其他，请补充

备注：

### 3.2 “这笔业务业绩应该算到谁头上”是否就是系统的核心问题？

`优先级：P0`

A. 是，且这是绝对主线  
B. 是，但还包含更重要的考核/统计目标  
C. 只是其中一个功能，系统主线另有重点  
D. 不确定

备注：

### 3.3 这套系统主要服务于哪个业务场景？

`优先级：P0`

A. 日常业务业绩认领  
B. 月度/季度考核归属处理  
C. 特定营销活动或专项统计  
D. 既服务日常，也服务周期性考核  
E. 其他，请补充

备注：

### 3.4 系统是否只负责“认领与审批”，不负责最终考核计算？

`优先级：P1`

A. 是，考核在别的系统或后续流程中完成  
B. 否，系统内部也承担部分考核计算  
C. 只承担部分考核准备工作  
D. 不确定

备注：

## 4. 业务范围与业务条线

### 4.1 下列业务条线中，哪些在原生产中真实使用过？

`优先级：P0`

请分别标注“长期使用 / 曾经使用 / 代码预留但未上线 / 不清楚”：

- 存款
- 贵金属
- 基金
- 理财/金融交易
- 第三方存管
- 代发工资
- 保险
- 贷款
- 公积金
- 其他，请补充

### 4.2 各业务条线之间的差异主要体现在哪些方面？

`优先级：P1`

A. 来源数据表不同  
B. 字段不同  
C. 认领规则不同  
D. 审批层级不同  
E. 数据可见范围不同  
F. 只是页面不同，规则基本一致  
G. 其他，请补充

备注：

### 4.3 是否存在“代码里有模块，但原生产并未真正启用”的情况？

`优先级：P0`

A. 有，而且不少  
B. 有，但较少  
C. 基本没有，代码里的模块大多都启用过  
D. 不确定

备注：

### 4.4 当前代码中存在较多考核、奖金、价格、汇总报表类页面，这些是否属于本次新系统迁移范围？

`优先级：P0`

A. 都属于核心范围，必须迁移  
B. 只迁移与认领结果直接相关的报表  
C. 只迁移查询展示，不迁移奖金/价格计算  
D. 暂不纳入第一期迁移  
E. 不确定

备注：

### 4.5 代码中出现 `2016qq` 等营销活动编号过滤，这类特殊活动在业务上是什么性质？

`优先级：P1`

参考数据上下文：

- 字段：`MARKETING_NO`
- 表/页面线索：`IBS.T1_VUCH_DETAIL.MARKETING_NO`、`IBS.T1_VUCH_EMP_RELA.MARKETING_NO`、`claim_dep.xml`、`claim_info.xml`、`claimed_change.xml`
- 当前观察：多个存贷款认领、查询、调整页面中存在 `COALESCE(MARKETING_NO,'') not in ('2016qq')` 或对 `2016qq` 单独分支处理

A. 真实存在的专项活动，需要保留特殊处理  
B. 历史一次性活动，新系统不需要保留  
C. 代表一类营销活动规则，新系统应抽象支持  
D. 只是临时代码或数据修正口径  
E. 不确定

备注：

## 5. 角色、组织与权限

### 5.1 原系统中最主要的业务角色有哪些？

`优先级：P0`

A. 认领人  
B. 初审人  
C. 终审人  
D. 调整发起人  
E. 调整审批人  
F. 系统管理员  
G. 业务管理员  
H. 其他，请补充

备注：

### 5.2 原系统的审批角色划分主要依据是什么？

`优先级：P0`

A. 按组织层级划分  
B. 按业务条线划分  
C. 按岗位/职责划分  
D. 按用户组配置，比较灵活  
E. 多种因素共同决定  
F. 不确定

备注：

### 5.3 数据可见范围主要按什么控制？

`优先级：P0`

A. 本人  
B. 本网点/本机构  
C. 所属支行  
D. 上级分行/总行  
E. 同时受组织层级和菜单权限控制  
F. 其他，请补充

备注：

### 5.4 菜单权限和数据权限在原系统里是否是两套逻辑？

`优先级：P1`

A. 是，菜单能看到不代表数据可见  
B. 基本是一套，菜单和数据权限绑定较紧  
C. 看模块而定  
D. 不确定

备注：

### 5.5 原生产中是否存在一人多角色、多审批身份并存的情况？

`优先级：P1`

A. 经常存在  
B. 偶尔存在  
C. 基本不存在  
D. 不确定

备注：

### 5.6 原系统是否支持“机构级归属”或“虚拟归属对象”？

`优先级：P0`

参考数据上下文：

- 字段：各认领表中的 `EMP_ID`、`EMP_NM`、`EMP_ORG_ID`、`VERIFY_EMP_ID`
- 表/代码线索：`IBS.T1_VUCH_EMP_RELA`、`IBS.GOLD_EMP_RELA`、`IBS.T1_FNC_TXN_EMP_RELA`、`SubmitClaimGold.java`、`SubmitClaimFncTxn.java`、`SubmitClaimLn.java`
- 当前观察：代码会插入 `EMP_ID='000000'` 的行；贷款提交中可出现 `VERIFY_EMP_ID='无需审批'`；部分逻辑把 `EMP_ID` 与机构表关联判断

A. 支持机构级归属，这是核心能力  
B. 支持虚拟归属对象，但只用于少数场景  
C. 这些只是技术占位，不代表业务对象  
D. 按业务条线不同  
E. 不确定

备注：

## 6. 菜单结构与模块分组

### 6.1 原生产系统的顶层菜单大致是怎样的？

`优先级：P0`

A. 只有“首页 + 系统管理”两大类  
B. 首页下再挂所有业务模块  
C. 按业务域分多个一级菜单  
D. 按“认领 / 审批 / 查询 / 管理”分组  
E. 其他，请补充

备注：

### 6.2 业务模块更接近以下哪种组织方式？

`优先级：P0`

A. 每个业务条线各自有认领、审批、查询页面  
B. 认领页面按条线分，审批页面部分合并  
C. 查询页面统一，认领审批分散  
D. 以组织角色为中心组织菜单，而不是按业务条线  
E. 其他，请补充

备注：

### 6.3 “我的认领”这类页面在原系统中是什么定位？

`优先级：P1`

A. 个人工作台/个人台账  
B. 只是一个辅助查询页  
C. 用户最常用的主入口之一  
D. 后期加的补充功能  
E. 不确定

备注：

## 7. 认领流程确认

### 7.1 认领流程的标准主线是否如下？

`优先级：P0`

A. 待认领 -> 暂存 -> 提交 -> 审批通过/驳回  
B. 待认领 -> 直接提交 -> 审批  
C. 待认领 -> 暂存 -> 提交 -> 初审 -> 终审  
D. 各业务条线差异较大  
E. 其他，请补充

备注：

### 7.2 “暂存”在业务上的作用是什么？

`优先级：P1`

A. 允许录入未完成时先保存  
B. 允许多人分步补充  
C. 只是技术上的中间状态  
D. 某些条线必须暂存，某些不需要  
E. 不确定

备注：

### 7.3 一笔业务记录是否允许拆分给多人按比例认领？

`优先级：P0`

A. 允许，且是核心业务能力  
B. 允许，但只在少数条线使用  
C. 原则上不允许，一般只认给一个人  
D. 视业务类型而定  
E. 不确定

备注：

### 7.4 认领比例的总和规则是什么？

`优先级：P0`

参考数据上下文：

- 字段：各认领表中的 `RATIO`
- 相关状态字段：`CLAIM_STATUS_ID`、`RVS_STATUS_ID`
- 代码线索：`Servervalidate_ClaimGold.java`、`Servervalidate_ClaimDep.java`、`Servervalidate_ClaimLn.java`、`Servervalidate_Fund.java`
- 当前观察：代码经常用 `100 - sum(ratio)` 计算剩余可认领比例；部分模块会把 `CLAIM_STATUS_ID in ('1','2','5','6','21')` 或调整状态下的 `RVS_RATIO` 纳入计算


A. 必须正好等于 100%  
B. 不能超过 100%，可以小于 100%  
C. 部分业务必须等于 100%，部分可以小于 100%  
D. 由审批时人工把关，不是硬校验  
E. 不确定

备注：

### 7.5 如果一笔业务已有部分比例被认领，后续认领是否允许继续占用剩余比例？

`优先级：P1`

A. 允许  
B. 不允许  
C. 仅特定业务条线允许  
D. 不确定

备注：

### 7.6 被驳回的认领记录后续通常如何处理？

`优先级：P1`

A. 原认领人可修改后重新提交  
B. 驳回即终止，需要重新发起  
C. 看模块而定  
D. 不确定

备注：

### 7.7 是否存在无需人工审批、提交后直接通过的认领场景？

`优先级：P0`

参考数据上下文：

- 字段：`CLAIM_STATUS_ID`、`VERIFY_EMP_ID`、`VERIFY_DT`
- 表/代码线索：`IBS.T1_VUCH_EMP_RELA`、`SubmitClaimLn.java`
- 当前观察：贷款提交代码中存在直接设置 `CLAIM_STATUS_ID='2'`、`VERIFY_EMP_ID='无需审批'` 的路径

A. 存在，且是正式业务规则  
B. 存在，但只限少数业务条线  
C. 原系统曾经支持，但生产基本不用  
D. 不存在，应全部人工审批  
E. 不确定

备注：

### 7.8 当认领对象是机构、团队或公共池时，比例规则是否与个人认领一致？

`优先级：P1`

A. 完全一致  
B. 不一致，机构/公共池有单独规则  
C. 只在贷款等特定业务中不一致  
D. 不确定

备注：

## 8. 审批流程与状态码确认

### 8.1 原系统审批模型更接近哪一种？

`优先级：P0`

A. 大多数模块单级审批  
B. 大多数模块两级审批  
C. 不同业务条线差异明显  
D. 还有三级及以上审批  
E. 不确定

备注：

### 8.2 `CLAIM_STATUS_ID='21'` 在业务上的标准含义更接近哪种？

`优先级：P0`

参考数据上下文：

- 字段：`CLAIM_STATUS_ID`
- 主要表：`IBS.T1_VUCH_EMP_RELA`
- 相关页面：`claim_verify_acc`、`claim_verify_accfs`、`claim_dep`
- 代码/配置线索：`ClaimVerifyAccServerCallBack.java` 将 `IBS.T1_VUCH_EMP_RELA.CLAIM_STATUS_ID` 从 `1` 更新为 `21`；`ClaimVerifyAccFsServerCallBack.java` 再将 `21` 更新为 `2`
- 当前页面文本线索：`claim_dep.xml` 中既出现过 `21-初审`，也出现过 `21-支行已审核`

A. 初审通过  
B. 支行审批通过  
C. 中间审批通过  
D. 特定条线的已通过，不一定代表统一含义  
E. 不确定

备注：

### 8.3 `CLAIM_STATUS_ID='31'` 在业务上的标准含义更接近哪种？

`优先级：P1`

参考数据上下文：

- 字段：`CLAIM_STATUS_ID`
- 主要表：`IBS.T1_VUCH_EMP_RELA`
- 相关页面：`claim_dep`、`claim_fund`
- 当前页面文本线索：`claim_dep.xml` 中出现 `31-初审未通过` 和 `31-支行未通过`；`claim_fund.xml` 中存在查询 `CLAIM_STATUS_ID in ('0','7','3','31')`

A. 初审驳回  
B. 中间审批驳回  
C. 特定条线自定义状态  
D. 不确定

备注：

### 8.4 `CLAIM_STATUS_ID='5'` 和 `CLAIM_STATUS_ID='6'` 是否在生产中真实使用？

`优先级：P0`

参考数据上下文：

- 字段：`CLAIM_STATUS_ID`
- 常见表：`IBS.T1_VUCH_EMP_RELA`、`IBS.GOLD_EMP_RELA`、`IBS.T1_FUND_*_EMP_RELA`、`IBS.T1_DF_SLRY_EMP_RELA`
- 当前页面文本线索：多个页面将 `5` 显示为 `超时自动审批`，将 `6` 显示为 `事中认领自动审核通过`
- 当前规则线索：比例校验经常把 `5`、`6` 作为已占用比例的有效状态

A. 两者都真实使用  
B. 只 `5` 用过  
C. 只 `6` 用过  
D. 基本只是代码预留  
E. 不确定

备注：

### 8.5 `CLAIM_STATUS_ID='5'` 和 `CLAIM_STATUS_ID='6'` 分别代表什么业务含义？

`优先级：P1`

A. 自动审批通过  
B. 超时自动通过  
C. 事中认领自动通过  
D. 按条线含义不同  
E. 不确定

备注：

### 8.6 驳回时填写“驳回原因”在原业务上是否是必填？

`优先级：P1`

参考数据上下文：

- 字段：`REMARK1` 常作为页面输入原因，`REMARK` 常拼接审批轨迹
- 相关页面：`claim_gold_vfy_no`、`verify_no_form`、`claimedRvs_no_formfs`
- 当前观察：多个“不通过”弹窗会要求输入原因并写回备注

A. 是，必须填写  
B. 否，可选  
C. 部分模块必填  
D. 不确定

备注：

### 8.7 `CLAIM_STATUS_ID='4'` 在业务上的含义是什么？

`优先级：P1`

参考数据上下文：

- 字段：`CLAIM_STATUS_ID`
- 主要表：`IBS.T1_VUCH_EMP_RELA`
- 相关页面：`claim_dep`、`claim_info`
- 当前页面文本线索：`claim_dep.xml` 中将 `4` 显示为 `4-撤销`；当前恢复链路尚未完整验证该状态如何产生

A. 认领人主动撤销  
B. 审批人或管理员撤销  
C. 系统自动撤销  
D. 历史预留状态，生产未使用  
E. 不确定

备注：

### 8.8 自动审批相关账号或标识的业务含义是什么？

`优先级：P1`

参考数据上下文：

- 字段：`VERIFY_EMP_ID`、`VERIFY_EMP_NM`
- 相关页面/代码：`claim_info.xml`、`SubmitClaimLn.java`
- 当前页面文本线索：`claim_info.xml` 中将 `ADMIN_HK` 显示为 `换卡自动审批`，将 `ADMIN_SZ` 显示为 `事中自动审批`
- 当前代码线索：`SubmitClaimLn.java` 中存在 `VERIFY_EMP_ID='无需审批'`

A. 正式代表自动审批结果  
B. 代表特定批处理任务  
C. 代表人工导入或历史修正  
D. 只是临时数据，不应迁移为业务规则  
E. 不确定

备注：

### 8.9 原系统是否支持批量审批？

`优先级：P1`

参考数据上下文：

- 相关页面：`claim_verify_acc`、`claim_verify_accfs`、`claim_verify_exs`、`emp_dx_fh_ver` 等
- 代码/页面线索：多个页面按钮或回调中出现 `批量通过`、`SELECTEDROW`、`选中数据批量通过`

A. 支持，且生产高频使用  
B. 支持，但只在少数模块使用  
C. 页面上有能力，但生产不常用  
D. 不支持  
E. 不确定

备注：

## 9. 调整流程确认

### 9.1 调整流程的业务前提是什么？

`优先级：P0`

参考数据上下文：

- 主表字段：`IBS.T1_VUCH_EMP_RELA.CLAIM_STATUS_ID`、`IBS.T1_VUCH_EMP_RELA.RVS_STATUS_ID`
- 调整日志字段：`IBS.T6_VUCH_EMP_RELA_CHANGE_LOG.NEW_CLAIM_STATUS_ID`
- 相关页面：`claimed_change`、`claimed_change_fs`
- 当前观察：`claimed_change` 主要对已有认领记录发起调整，保存写 `RVS_RATIO`，提交推进 `RVS_STATUS_ID` 和 `NEW_CLAIM_STATUS_ID`

A. 只有已审批通过的认领才能调整  
B. 已提交待审批的也能调整  
C. 认领后的任意状态都可调整  
D. 按条线不同  
E. 不确定

备注：

### 9.2 调整流程是否独立于初始认领流程？

`优先级：P0`

A. 是，属于一条独立的后置流程  
B. 否，本质上还是原认领流程的一部分  
C. 部分独立  
D. 不确定

备注：

### 9.3 调整审批通过后，原记录如何处理？

`优先级：P0`

参考数据上下文：

- 主表字段：`IBS.T1_VUCH_EMP_RELA.RATIO`、`IBS.T1_VUCH_EMP_RELA.RVS_RATIO`、`IBS.T1_VUCH_EMP_RELA.RVS_STATUS_ID`
- 日志表字段：`IBS.T6_VUCH_EMP_RELA_CHANGE_LOG.NEW_RATIO`、`NEW_CLAIM_STATUS_ID`
- 代码线索：`ClaimedRvsFsServerCallBack.java` 和 `claimed_change_fs.xml` 中审批通过会设置 `RVS_STATUS_ID='2'`，并将 `RATIO = RVS_RATIO`

A. 直接覆盖原归属比例  
B. 保留原记录，同时生成新生效版本  
C. 同时保留主记录和调整日志  
D. 不确定

备注：

### 9.4 调整是否也存在多级审批？

`优先级：P1`

参考数据上下文：

- 字段：`RVS_STATUS_ID`、`NEW_CLAIM_STATUS_ID`
- 当前观察：部分审批代码会把调整状态推进到 `21`，再由后续审批推进到 `2`

A. 是  
B. 否  
C. 只有部分模块有  
D. 不确定

备注：

### 9.5 调整流程中是否允许“撤销认领”或将比例调整为 0？

`优先级：P1`

参考数据上下文：

- 字段：`RATIO`、`RVS_RATIO`、`RVS_STATUS_ID`
- 相关页面：`claimed_change`
- 当前页面文本线索：`claimed_change.xml` 中出现“认领撤销”相关备注拼接

A. 允许，且属于正式调整能力  
B. 允许，但需要特殊审批  
C. 不允许，只能改比例不能撤销  
D. 按业务条线不同  
E. 不确定

备注：

### 9.6 调整通过后，历史版本应如何被查询？

`优先级：P1`

参考数据上下文：

- 主表：`IBS.T1_VUCH_EMP_RELA`
- 日志表：`IBS.T6_VUCH_EMP_RELA_CHANGE_LOG`
- 查询页面：`my_claimed` 的“我的调整”页签、`claimed_change` 的认领信息区

A. 只展示最新生效结果  
B. 展示最新结果，同时可查历史调整日志  
C. 每次调整生成独立版本，历史版本同等重要  
D. 不确定

备注：

## 10. 个人查询与工作台

### 10.1 “我的认领”类页面的核心价值是什么？

`优先级：P1`

参考数据上下文：

- 页面：`my_claimed`、`my_claim_info`、`remark_info`
- 主查询表：`IBS.T1_VUCH_EMP_RELA`
- 调整查询表：`IBS.T6_VUCH_EMP_RELA_CHANGE_LOG`
- 当前观察：`my_claimed` 包含“我的存贷款认领”“我的调整”“我的贵金属认领”“我的第三方存管认领”“我的基金认领”等页签

A. 查询本人认领历史  
B. 跟踪审批进度  
C. 查询本人调整历史  
D. 统一查看多条线归属结果  
E. 以上都有

备注：

### 10.2 原系统是否要求个人查询页按业务条线分栏/分页展示？

`优先级：P1`

A. 是，分条线展示是必须保留的  
B. 不必须，统一列表也可以  
C. 只要能筛选业务条线即可  
D. 不确定

备注：

### 10.3 个人查询页是否只展示最终通过的数据？

`优先级：P1`

参考数据上下文：

- 字段：`CLAIM_STATUS_ID`、`RVS_STATUS_ID`、`NEW_CLAIM_STATUS_ID`
- 页面：`my_claimed`
- 当前观察：个人查询页会展示状态名称和审批/调整日期，不只是最终结果字段

A. 是  
B. 否，也展示待审批和驳回状态  
C. 看不同页签而定  
D. 不确定

备注：

## 11. 考核、奖金、报表与价格类模块

### 11.1 原系统中的考核期控制是否是核心业务规则？

`优先级：P0`

参考数据上下文：

- 字段：`EXAM_YEAR`、`EXAM_Q`、`STUS`
- 页面/代码线索：`emp_dx.xml`、`emp_dx_fh_ver.xml`、`emp_exs_xz.xml`、`branch_dx.xml`、`InterceptorEmpGdqMod.java`、`InterceptorOtherChgMod.java`
- 当前观察：代码中多处出现“不是考核期不能修改”“请在考核期内对考核期数据进行维护，过期数据不能补录”等提示

A. 是，所有认领/调整/评分都必须受考核期控制  
B. 是，但只影响考核评分、奖金、价格类模块  
C. 只影响少数维护页面  
D. 不是核心规则，可在新系统中弱化  
E. 不确定

备注：

### 11.2 奖金、考核系数、价格类模块与认领结果是什么关系？

`优先级：P0`

参考数据上下文：

- 页面线索：`bonus_sum*.xml`、`BanusShareRatio*.xml`、`branch_ratio.xml`、`commission_ratio.xml`、`price*.xml`、`credit_card_price.xml`
- 字段线索：`BONUS_TYPE_ID`、`PSTN_TYPE`、`SUB_PSTN_TYPE`、`RATION`、`EXAM_YEAR`、`EXAM_Q`

A. 直接使用认领结果计算奖金或绩效  
B. 只是展示或查询，不负责计算  
C. 部分计算在本系统，部分在下游系统  
D. 历史功能，当前业务不再使用  
E. 不确定

备注：

### 11.3 报表导出是否是迁移时必须保留的能力？

`优先级：P1`

参考数据上下文：

- 页面配置线索：多个报表 XML 中存在 `dataexport="richexcel"` 或类似导出能力
- 相关页面类型：业绩汇总、奖金汇总、客户活动明细、认领查询

A. 必须保留，且格式要尽量一致  
B. 必须保留，但格式可以重做  
C. 只保留核心报表导出  
D. 第一期可以不保留  
E. 不确定

备注：

### 11.4 定性考核评分、分行审核、员工/机构汇总类页面是否属于本系统主业务域？

`优先级：P1`

参考数据上下文：

- 页面线索：`emp_dx.xml`、`emp_dx_fh.xml`、`emp_dx_fh_ver.xml`、`branch_dx.xml`、`emp_gdq.xml`
- 当前观察：这些页面存在评分、提交、分行审核、批量通过等动作，与纯查询报表不同

A. 属于，与认领结果共同组成完整绩效流程  
B. 属于独立考核子系统，但与认领数据有关  
C. 只是报表/辅助功能  
D. 历史遗留，新系统可不迁移  
E. 不确定

备注：

## 12. 模块边界与产品结构

### 12.1 新系统迁移时，以下哪种模块边界最需要保留？

`优先级：P0`

A. 按业务条线保留认领模块边界  
B. 按审批层级保留审批模块边界  
C. 保留独立的调整模块  
D. 保留独立的个人查询模块  
E. 上述都需要保留

备注：

### 12.2 哪些模块虽然代码不同，但在业务上可以视为同一类能力？

`优先级：P1`

A. 所有认领模块  
B. 所有审批模块  
C. 所有调整模块  
D. 所有个人查询模块  
E. 仍需按条线分别看待  
F. 其他，请补充

备注：

### 12.3 对原页面名称、菜单名称、业务术语，迁移时应保持到什么程度？

`优先级：P1`

A. 名称和术语必须尽量完全一致  
B. 核心业务术语一致即可，菜单可重组  
C. 只要业务含义一致，名称可以产品化重命名  
D. 按模块决定  
E. 不确定

备注：

## 13. 上下游系统与外部集成

### 13.1 上游业务数据主要来自哪里？

`优先级：P0`

A. 柜面/核心业务系统  
B. CRM/营销系统  
C. 批量导入  
D. 多个系统汇聚  
E. 不确定

备注：

### 13.2 上游数据进入本系统的主要方式是什么？

`优先级：P0`

A. 定时批量导入  
B. 实时接口同步  
C. 人工导入  
D. 多种方式并存  
E. 不确定

备注：

### 13.3 本系统的结果主要被谁消费？

`优先级：P0`

A. 后续考核系统  
B. 报表/统计系统  
C. 人工导出使用  
D. 本系统内部查询即可  
E. 多方消费  
F. 不确定

备注：

### 13.4 本系统是否真实接入过外部单点登录？

`优先级：P1`

参考数据上下文：

- 代码线索：`com.customer.security.WSLogin`
- 接口线索：`interfaceservice.InterfaceServiceServiceStub.ssoTicketValidate`
- 当前观察：代码会将 token 组装为 XML，调用 `ssoTicketValidate`，验证成功后写入 `user_id`、`org_id`、`org_lvl` 等 session 信息

A. 是，生产中真实用过  
B. 有接入代码，但未正式启用  
C. 从未接入过，只是预留  
D. 不确定

备注：

### 13.5 审批流程是否依赖外部 BPM/流程平台？

`优先级：P0`

A. 不依赖，完全系统内实现  
B. 依赖外部 BPM  
C. 部分模块依赖  
D. 不确定

备注：

### 13.6 旧系统是否存在定时任务、批处理或日终/月末处理？

`优先级：P0`

参考数据上下文：

- 代码/页面线索：当前源码中审批状态多由页面按钮和 Java 回调直接更新，但提示文案中多次出现“批量原因”“新开户的凭证号，请明日再试”
- 需要确认：这些“批量”是上游入库批处理、自动审批批处理、还是业务数据刷新周期

A. 有，且与认领/审批状态直接相关  
B. 有，但主要用于报表或数据同步  
C. 有历史批处理，但新系统可以重新设计  
D. 没有，主要由人工页面操作驱动  
E. 不确定

备注：

## 14. 历史数据与运行口径

### 14.1 原生产中，是否存在“只有结构，没有完整历史明细”的条线？

`优先级：P1`

A. 有  
B. 没有  
C. 不确定

备注：

### 14.2 原系统是否强依赖历史数据连续性？

`优先级：P1`

A. 是，很多功能依赖历史累计  
B. 部分模块依赖  
C. 大部分模块只依赖当前业务数据  
D. 不确定

备注：

### 14.3 迁移时，如果先用 mock 数据跑通主链路，是否会影响业务规则确认？

`优先级：P1`

A. 不影响主规则判断  
B. 会影响部分边界规则  
C. 会严重影响对真实业务的判断  
D. 不确定

备注：

### 14.4 历史备注字段中拼接的审批轨迹，迁移后是否需要结构化保留？

`优先级：P1`

参考数据上下文：

- 字段：`REMARK`、`REMARK1`
- 表线索：`IBS.T1_VUCH_EMP_RELA`、`IBS.GOLD_EMP_RELA`、`IBS.T1_FUND_*_EMP_RELA`、`IBS.T6_VUCH_EMP_RELA_CHANGE_LOG`
- 代码/页面线索：`SubmitClaimGold.java`、`SubmitClaimFncTxn.java`、`claim_info.xml`、`com.ljbank.market.Util`
- 当前观察：旧代码中大量将“认领时间、认领工号、审批时间、审批工号、审批理由”等拼接进备注字段

A. 必须结构化迁移为审计日志  
B. 保留原备注文本即可  
C. 新系统只保留新发生操作的结构化日志，历史文本归档  
D. 不需要迁移历史备注  
E. 不确定

备注：

## 15. 迁移约束确认

### 15.1 以下哪些内容在新系统中必须保持与旧系统一致？

`优先级：P0`

A. 角色边界  
B. 状态流转  
C. 归属比例规则  
D. 审批层级  
E. 数据可见范围  
F. 审计留痕  
G. 页面结构与菜单名称  
H. 其他，请补充

备注：

### 15.2 以下哪些内容在新系统中可以调整？

`优先级：P0`

A. 页面交互方式  
B. 菜单组织方式  
C. 技术实现方式  
D. 数据库物理设计  
E. 权限实现技术方案  
F. 其他，请补充

备注：

### 15.3 如果新系统与旧系统出现差异，产品上最不能接受的是哪一类差异？

`优先级：P0`

A. 业务结果不一致  
B. 审批链路不一致  
C. 权限范围不一致  
D. 状态定义不一致  
E. 页面样式不一致  
F. 其他，请补充

备注：
